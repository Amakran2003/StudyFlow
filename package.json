{
    "name": "studyflow-monorepo",
    "version": "1.0.0",
    "scripts": {
      "dev": "concurrently \"cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 --reload\" \"cd frontend && npm start\""
    },
    "devDependencies": {
      "concurrently": "^7.0.0"
    }
  }
  